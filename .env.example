# Copy this file to .env.local and fill in values.
# IMPORTANT:
# - These values are exposed to the browser (Vite `VITE_` prefix), so do NOT put secrets here.

# Enoki (zkLogin) public API key from Enoki Portal
VITE_ENOKI_PUBLIC_KEY=

# Google OAuth Client ID (Web application)
VITE_GOOGLE_CLIENT_ID=

# Sui Move package id for the deployed ticket module
VITE_TICKET_PACKAGE_ID=

# Shared EventRegistry object id created by init_registry (optional)
VITE_TICKET_REGISTRY_ID=
